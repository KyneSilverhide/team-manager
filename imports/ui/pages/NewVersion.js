import React from 'react';
import VersionEditor from '../components/VersionEditor.js';

const NewTeam = () => (
  <div className="NewTeam">
    <h4 className="page-header">Ajout d'une nouvelle version</h4>
    <VersionEditor />
  </div>
);

export default NewTeam;
